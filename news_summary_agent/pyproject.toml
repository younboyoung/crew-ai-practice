[project]
name = "news_summary_agent"
version = "0.1.0"
description = "news_summary_agent using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.3.0"
]

[project.scripts]
news_summary_agent = "news_summary_agent.main:run"
run_crew = "news_summary_agent.main:run"
train = "news_summary_agent.main:train"
replay = "news_summary_agent.main:replay"
test = "news_summary_agent.main:test"
run_with_trigger = "news_summary_agent.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
